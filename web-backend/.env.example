# Server
PORT=3001
NODE_ENV=development

# Database (same DB as mobile backend)
DATABASE_URL=postgresql://admin:secret@localhost:5432/greencollect

# JWT (use same secret as mobile backend)
JWT_SECRET=your-super-secret-key
JWT_EXPIRES_IN=8h
